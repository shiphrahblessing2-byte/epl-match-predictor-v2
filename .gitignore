# Secrets — NEVER commit
.env
*.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
*.so
.pytest_cache/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Data (too large for git — use Google Drive)
data/raw/
data/processed/
data/*.db
*.parquet
*.duckdb

# Data files — too large for git, regenerated by pipeline
data/cache/
data/interim/
data/training/

# Models (use GitHub Releases for model artifacts)
models/*.zip
models/*.pkl
models/archive/

# MLflow runs (large)
mlruns/

# Optuna DB
optuna/*.db

# Logs
logs/
*.log

# Venv
venv/
.venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Secrets baseline (commit this one — it's safe)
# .secrets.baseline  <- do NOT ignore this

